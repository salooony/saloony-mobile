import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  sectionTitleWrap: {
    paddingHorizontal: 16,
    paddingVertical: 14,
  },
  sectionTitle: {
    fontWeight: '800',
    fontSize: 18,
  },
});

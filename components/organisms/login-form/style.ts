import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  forgetText: { 
    flex: 1, 
    alignItems: 'flex-end' 
  },
  buttonContainer: {
    flex: 1,
    alignItems: 'center',
    marginTop: 30,
  },
});
